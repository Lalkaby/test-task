create extension if not exists "uuid-ossp";
CREATE TABLE IF NOT EXISTS temp (
                                    id int generated always as identity,
                                    title varchar(10),
    temp_uuid uuid default uuid_generate_v4()
    );

copy temp(title)
    from '/mnt/d/JetBrains_Projects/Intellij_projects/test-task/src/main/resources/db.migration/data-temp.csv'
    DELIMITER ','
    CSV HEADER ;
